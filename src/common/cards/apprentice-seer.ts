import Card from '../card';

class ApprenticeSeer extends Card {
  constructor() {
    super('Apprentice Seer', 'Villager', 'View one center card.', true);
  }
}

export default new ApprenticeSeer();
